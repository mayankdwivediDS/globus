# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/

# Virtualenvs
.venv/
venv/

# Local config / secrets
.env
.env.*
!.env.example

# Local metadata dumps (per-member Drive/Gmail listing JSON — no file
# content, but still private data; never commit)
/local_data/

# Local scratch / test outputs
/tmp/
*.log
.coverage
htmlcov/
globus-truth.db
globus-truth.db-shm
globus-truth.db-wal
judge-artifacts/
outcome-challenges/

# Local data (FAISS indexes, metadata, runtime files - NEVER commit)
/local_data/
/var/lib/
*.faiss
*.meta.json

# Editor
.vscode/
.idea/
*.swp
.DS_Store
.now

# Node (test tooling, e.g. scripts/playwright_voice_test)
node_modules/